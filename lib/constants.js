// Canonical name for this plugin
const PLUGIN_NAME = "KevinMiddleware";

// Build statuses
const FIRST_BUILD = "first-build";
const BUILDING = "building";
const ERROR = "error";
const DONE = "done";

module.exports = {
    PLUGIN_NAME,

    FIRST_BUILD,
    BUILDING,
    ERROR,
    DONE,
};
